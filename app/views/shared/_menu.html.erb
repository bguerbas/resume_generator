<% if user_signed_in? %>
  <%= link_to t('devise.actions.edit'), edit_user_registration_path, :class => 'navbar-link' %> |
  <%= link_to t('devise.actions.logout'), destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
<% end %>